<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.EmployeeMapper">

    <select id="selectAll" resultType="com.example.entity.Employee">
        select `employee`.* from `employee`

        <where>
			<if test="username != null and username != ''">
				and `employee`.`username` like concat ('%', #{username}, '%')
			</if>
			<if test="sex != null and sex != ''">
				and `employee`.`sex` like concat ('%', #{sex}, '%')
			</if>
			<if test="phone != null and phone != ''">
				and `employee`.`phone` like concat ('%', #{phone}, '%')
			</if>

        </where>
    </select>

    <select id="selectById" resultType="com.example.entity.Employee">
        select * from employee where id = #{id}
    </select>

    <delete id="deleteById">
        delete from employee where  id = #{id}
    </delete>

</mapper>